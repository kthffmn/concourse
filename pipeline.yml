resources:
- name: concourse
  type: git
  source:
    uri: https://github.com/kthffmn/concourse.git
    branch: master
jobs:
- name: hello-world
  plan:
  - get: concourse
    trigger: true
  - task: say-hello
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      run:
        path: ./say-hello.sh
